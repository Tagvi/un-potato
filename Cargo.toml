[package]
name = "un-potato"
version = "0.2.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
notify-rust = "^4"
tokio = {version = "1.24.2", features = ["time", "rt", "macros", "signal", "rt-multi-thread"]}
